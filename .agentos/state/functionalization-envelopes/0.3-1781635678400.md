Continue AgentOS functionalization: complete task 0.3 — Functional profile env block.

Prior task 0.2 is marked done. Run phase gates with `pnpm functional:gates` before calling `pnpm functional:next`.

Scope (do not expand):
Add docs/functional-profile.md and .env.functional.example with flags for real execution path (FEATURE_AGENT_LLM, AGENTOS_API_REQUIRE_AUTH, etc.).

Files (primary touch surface):
- docs/functional-profile.md
- .env.functional.example

Gates to satisfy:
- [functionality] pnpm env:check
- [functionality] pnpm typecheck
- [functionality] pnpm test

Rules: AGENTS.md, minimal diff, match repo conventions, add tests for security paths, no commits unless user asks.

When finished report: done / blocked, files changed, gate command results, and whether to run `pnpm functional:next`.