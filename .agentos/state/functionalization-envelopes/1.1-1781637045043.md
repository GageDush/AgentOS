Continue AgentOS functionalization: complete task 1.1 — Global auth preHandler.

Prior task 0.4 is marked done. Run phase gates with `pnpm functional:gates` before calling `pnpm functional:next`.

Scope (do not expand):
Add requireAuth/requireOperator Fastify preHandlers; protect mutating routes (/missions POST, /runs/*, /approvals/*, /worker/process, /events WS, Discord admin). Public allowlist: /health, /auth/*, /media/agents. Use AGENTOS_API_REQUIRE_AUTH env (functional profile true).

Files (primary touch surface):
- apps/api/src/auth-guard.ts
- apps/api/src/index.ts
- apps/api/src/session.ts

Gates to satisfy:
- [functionality] pnpm --filter @agentos/api exec vitest run src/auth-guard.test.ts
- [security] pnpm functional:gate:auth
- [functionality] pnpm mission:smoke
- [security] pnpm functional:gate:auth
- [security] pnpm functional:gate:scraper

Rules: AGENTS.md, minimal diff, match repo conventions, add tests for security paths, no commits unless user asks.

When finished report: done / blocked, files changed, gate command results, and whether to run `pnpm functional:next`.