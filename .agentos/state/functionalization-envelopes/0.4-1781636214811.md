Continue AgentOS functionalization: complete task 0.4 — Hygiene scripts parity.

Prior task 0.3 is marked done. Run phase gates with `pnpm functional:gates` before calling `pnpm functional:next`.

Scope (do not expand):
Add root lint script or document omission; add gateway alias ↔ sandbox parity test if missing.

Files (primary touch surface):
- package.json
- apps/gateway

Gates to satisfy:
- [functionality] pnpm typecheck
- [functionality] pnpm typecheck
- [functionality] pnpm test

Rules: AGENTS.md, minimal diff, match repo conventions, add tests for security paths, no commits unless user asks.

When finished report: done / blocked, files changed, gate command results, and whether to run `pnpm functional:next`.