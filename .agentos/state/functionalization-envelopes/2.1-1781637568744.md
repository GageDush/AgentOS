Continue AgentOS functionalization: complete task 2.1 — Wire routeLlmCall into callAgentLlm.

Prior task 1.3 is marked done. Run phase gates with `pnpm functional:gates` before calling `pnpm functional:next`.

Scope (do not expand):
Replace prompt-echo default with packages/llm-router when FEATURE_AGENT_LLM=true; preserve explicit mock fallback.

Files (primary touch surface):
- packages/agents/src/llm.ts
- packages/llm-router/src/index.ts

Gates to satisfy:
- [functionality] pnpm --filter @agentos/agents test
- [functionality] pnpm llm:smoke
- [functionality] pnpm functional:smoke
- [security] pnpm functional:gate:auth

Rules: AGENTS.md, minimal diff, match repo conventions, add tests for security paths, no commits unless user asks.

When finished report: done / blocked, files changed, gate command results, and whether to run `pnpm functional:next`.