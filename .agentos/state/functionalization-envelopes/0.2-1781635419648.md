Continue AgentOS functionalization: complete task 0.2 — API auth route matrix.

Prior task 0.1 is marked done. Run phase gates with `pnpm functional:gates` before calling `pnpm functional:next`.

Scope (do not expand):
Document every Fastify route as public-read, authenticated-read, authenticated-mutate, operator-admin, or internal-only in docs/architecture/api-auth-matrix.md.

Files (primary touch surface):
- docs/architecture/api-auth-matrix.md
- apps/api/src/index.ts
- apps/api/src/scraper/routes.ts

Gates to satisfy:
- [functionality] node scripts/functionalization/check-auth-matrix.mjs
- [functionality] pnpm typecheck
- [functionality] pnpm test

Rules: AGENTS.md, minimal diff, match repo conventions, add tests for security paths, no commits unless user asks.

When finished report: done / blocked, files changed, gate command results, and whether to run `pnpm functional:next`.