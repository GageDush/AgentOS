# AgentOS — Risk Register

**Repository root:** `C:\Users\gaged\Documents\AgenOS`
**Date:** 2026-06-16 · Review-only forensic audit

Severity: Critical / High / Medium / Low. Likelihood: High / Medium / Low. Owner Type: Eng (engineering), Sec (security), Prod (product), Ops (operations/infra).

| # | Risk | Severity | Likelihood | Impacted Area | Evidence | Mitigation | Owner Type |
|---|------|----------|-----------|---------------|----------|------------|-----------|
| R1 | **Unauthenticated operational API** — only `/auth/me` & `/auth/success` enforce the session; `/missions`, `/runs/*`, `/worker/process`, `/approvals/*`, `/discord/bootstrap`, `/events` WS are open to anyone who can reach the port | Critical | High | Backend / Security | No global auth hook; `actingOperatorId` falls back to default operator (`apps/api/src/index.ts:155-157`); only 2 routes 401 | Add one Fastify `preHandler` requiring a valid session on all mutating + sensitive routes; allow-list public ones explicitly | Sec/Eng |
| R2 | **Unauthenticated, SSRF-capable scraper** driving headless Chrome with `--no-sandbox`; arbitrary URLs incl. internal/loopback, then bytes downloadable | Critical | High | Backend / Security | `apps/api/src/scraper/routes.ts:34` only `new URL()` validation; `downloader.ts:940-944` `--no-sandbox`; registered with no auth (`index.ts:161`); **zero tests** | Require auth; add SSRF allow-list / block private ranges; drop `--no-sandbox` or isolate; add tests | Sec/Eng |
| R3 | **Live secrets at rest in `.env`** (OpenAI, Anthropic, `GH_TOKEN`, Discord bot token, Cursor key, IcePanel, SESSION_SECRET) on a developer machine; also risk of capture in backup zips | High | Medium | Security / Ops | Integrations audit read `.env`; real non-empty key lengths. Backups: `Documents/AgentOS-Backups/*.zip`, `Downloads/AgentOS_Project_Bundle.zip` | Move to a secret manager; rotate keys; verify `.env` excluded from all backups/zips; the OpenAI/Anthropic keys are currently **unused by code** — remove or justify | Sec/Ops |
| R4 | **"Unclear agent runtime" — intelligence is mock by default** and the real cloud router is unwired, so "make it real" is a refactor, not a flag flip; product can be over-sold as autonomous | High | High | Agent runtime / Product | Pipeline only runs in mock branch (`packages/runtime/src/index.ts:70-79,1277`); `callAgentLlm` prompt-echo (`packages/agents/src/llm.ts:78-97`); router admits it's unwired (`packages/llm-router/src/index.ts:11-13`) | Wire `routeLlmCall` into the agent path; run pipeline in non-mock path; label mock clearly in UI | Eng/Prod |
| R5 | **Persistence concurrency / full-DB-rewrite** — `mutate()` and every `*Bundle` write delete+reinsert all 21 tables; 3 processes share one SQLite file → last-writer-wins clobbering & write amplification | High | Medium | Persistence | `saveDatabase` `packages/persistence/src/index.ts:3894-3961`; bundles route through `mutate()`; API+worker+scheduler share singleton path | Port hot writes to per-row SQL in transactions; add `busy_timeout`/`BEGIN IMMEDIATE`; remove blob rewrites | Eng |
| R6 | **No migrations; FKs OFF; zero indexes; unbounded append-only tables** — schema can't evolve safely; queries are full-scan + `JSON.parse`; `audit_events`/`mission_logs`/`chat_messages` grow forever | High | High | Persistence | `pragma("foreign_keys = OFF")` (`:1884`); no `CREATE INDEX`; `db-migrate.mjs` is a JSON→SQLite importer, not a migrator; dual schema (`:120-412` vs `:3819-3843`) | Adopt Drizzle Kit migrations; enable FKs; index hot columns; add retention/pruning; single schema source | Eng |
| R7 | **Architecture drift — mock-mode is load-bearing** — turning off mock *reduces* agent behavior; the impressive pipeline exists mainly to render in mock | Medium | High | Architecture | `isMockAgentExecutionEnabled` gates the whole pipeline (`runtime:1277`); non-mock path skips agents | Invert: make non-mock the rich path; treat mock as a deterministic test double | Eng |
| R8 | **Too much mock UI mislabeled "LIVE"** — home analytics/providers/integrations widgets are `Math.random`/static constants but badged "LIVE / Refreshed live from the runtime" | Medium | High | UI/UX / Product | `ForgeHome.tsx:706-829,741`; `forge-home-data.tsx:450-452` (`ROUTED_TODAY=4_720_000`) | Wire to `/usage` or relabel as "sample"; don't ship fake metrics as live | Prod/Eng |
| R9 | **Unsafe command execution surface if flags flipped naively** — gateway passes full `process.env` (incl. secrets) to children; allow-list ⇄ alias map drift; broadening tools without a real sandbox is dangerous | Medium | Medium | Sandbox / Security | `apps/gateway/src/index.ts:42` `env: process.env`; alias/policy mismatch (`index.ts:13-26` vs `sandbox/index.ts:24-33`) | Minimal child env; reconcile lists w/ a parity test; design a real sandbox before broadening | Sec/Eng |
| R10 | **Missing test coverage on highest-risk surfaces** — scraper (0 tests), Discord gateway WS reconnect, `gh` spawn paths, Cursor real SDK, LiteLLM cloud lane; e2e is non-blocking, semgrep gate disabled in CI | Medium | Medium | Testing | UI/integration audits; `.github/workflows/ci.yml:34-41` (`SEMGREP_GATE=false`), e2e `continue-on-error: true` | Add scraper + gateway-WS tests; make semgrep gate + e2e blocking once green | Eng |
| R11 | **Cost / token blow-ups** — real usage/cost is hardcoded `0` in the live path, so budget gates can't actually see spend once cloud is enabled; paid keys present | High | Medium | Token-manager / Cost | `completeRunBundle` usageEvent tokens/cost = 0 (`packages/runtime`); real counts only in unwired `routeLlmCall` | Wire real token/cost from `routeLlmCall`; verify budget hard-stop before enabling cloud | Eng/Ops |
| R12 | **Unbounded scope / feature creep** — Discord houses, Cursor bridge, scraper, wiki, IcePanel diagrams, tunnels, project-wave runner: many surfaces, thin depth on the core (real execution) | Medium | High | Product | Breadth of `scripts/*` (60+), many integrations vs mock core | Freeze new surfaces until the local real-execution path is proven + benchmarked | Prod |
| R13 | **Fragile UI/backend wiring** — synthetic progress %/confidences; home polls separately from the WS; brittle e2e text-match selectors that `skip` on missing data (silent pass) | Low | Medium | UI/UX / Testing | `dashboard-adapters.ts` synthetic values; `e2e/acceptance.spec.ts:28,34,53` | Derive metrics from real data; replace text selectors with `data-testid`; fail (not skip) on missing data | Eng |
| R14 | **Dependency / config hazards** — no root `lint` script though `pnpm lint` is allow-listed; Node 22 documented but runs on 24; Postgres/Redis in compose but intentionally unused | Low | Low | Config | `package.json` (no `lint`); `docker-compose.yml` pg/redis; CLAUDE.md "don't switch to Postgres" | Add `lint` or remove from allow-list; pin Node engines; prune unused compose services or document | Eng |
| R15 | **Auth/security gaps beyond R1** — 365-day session cookies; permissive CORS `origin:true, credentials:true`; Discord admin ops gated only on env-var presence (any caller can restructure the guild) | Medium | Medium | Security | `session.ts:14` (max-age), `index.ts:159` (CORS), `index.ts:1299-1301` (env-gated bootstrap) | Shorten/rotate sessions; restrict CORS origins; gate Discord admin on operator identity | Sec |
| R16 | **Poor observability for ops** — no metrics/tracing/alerting; `worker`/`gateway` health are hardcoded "online" not probed; logs are stdout JSON only | Medium | Medium | Observability / Ops | `index.ts:213-219,1230-1234` hardcoded online | Real health probes; add metrics endpoint + alerting before hosting | Ops/Eng |
| R17 | **Local vs cloud confusion** — README/CLAUDE say SQLite-only/local-first, but `.env`/compose/tunnel target hosted `flous.dev` with Postgres URL and Discord prod redirects; Postgres adapter throws | Medium | Medium | Architecture / Ops | `.env.example` `DATABASE_URL` postgres + `flous.dev`; `adapters/postgres.ts` not implemented | Pick a clear local→host story; finish Postgres adapter before pointing prod at it | Prod/Eng |
| R18 | **Weak deployment story** — host-specific PowerShell/tunnel scripts hardcode `C:\Users\gaged\.cloudflared`; no repeatable/containerized deploy; no deploy step in CI | Medium | Medium | Deployment / Ops | `scripts/repair-cloudflare-tunnel.ps1`, `infra/cloudflared/config.yml.template` | Containerize the stack (compose exists); parameterize host paths; add a deploy pipeline | Ops |
| R19 | **Performance uncertainty** — no app-perf benchmarks; concurrent agent runs untested against the full-rewrite persistence; unknown behavior under load | Medium | Medium | Performance | No latency/throughput bench; R5 write pattern | Add the benchmark plan in `AGENTOS_ARCHITECTURE_MAP.md §11`; load-test before multi-worker | Eng |
| R20 | **Discord/mobile feature creep vs no mobile surface** — mobile "control" is documented intent but only CSS-responsive; risk of building a mobile app before the core is real | Low | Medium | Product | UI audit §5 (no mobile route) | Defer mobile; rely on responsive web until core is proven | Prod |

### Top 5 to act on first
1. **R1 + R2** — add authZ and lock down the scraper (one `preHandler` + SSRF allow-list). Both Critical, both quick relative to impact.
2. **R3 / R11** — secret hygiene + real cost accounting before any cloud key is exercised.
3. **R5 / R6** — persistence hardening (migrations, FKs, indexes, kill the full-DB-rewrite) before hosting or multi-worker.
4. **R4 / R7** — wire the real LLM router and make the non-mock path the rich one (turns "mock" into "real").
5. **R8** — stop labeling synthetic widgets "LIVE."
