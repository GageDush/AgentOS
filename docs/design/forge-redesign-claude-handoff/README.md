# Forge redesign — Claude prompt pack

Two files, two purposes:

| File | Use |
|------|-----|
| **`chatbox.txt`** | Paste into Claude Design / Cowork chat |
| **`prompt.md`** | Full handoff reference — attach or `@` in Claude, share with Cursor/Codex |

When requesting a new prompt from Cursor, ask for **both** deliverables.
