node_modules/
.next/
dist/
coverage/
.env
*.log
apps/*/.next/
apps/*/dist/

# Local AgentOS runtime state (SQLite + legacy JSON)
.agentos/state/*.db
.agentos/state/*.db-wal
.agentos/state/*.db-shm
.agentos/state/agentos-local.json

# Local AgentOS runtime state (keep profiles/contracts tracked)
.agentos/state/*.db
.agentos/state/*.db-wal
.agentos/state/*.db-shm
.agentos/state/agentos-local.json
