node_modules/
.next/
dist/
coverage/
.env
*.log
apps/*/.next/
apps/*/dist/

# Local AgentOS runtime state (SQLite + legacy JSON)
.agentos/state/*.db
.agentos/state/*.db-wal
.agentos/state/*.db-shm
.agentos/state/agentos-local.json
.agentos/state/discord-guild.json
.agentos/state/discord-chat-rooms.json
.agentos/state/discord-registry.json
.agentos/stops/

# Local AgentOS backups (zipped runtime snapshots; may contain .env)
.agentos/backups/

# TypeScript incremental build info
*.tsbuildinfo
