node_modules/
.next/
dist/
coverage/
.env
*.log
apps/*/.next/
apps/*/dist/

# Local AgentOS runtime state (SQLite + legacy JSON)
.agentos/state/*.db
.agentos/state/*.db-wal
.agentos/state/*.db-shm
.agentos/state/agentos-local.json
.agentos/state/discord-guild.json
.agentos/state/discord-chat-rooms.json
.agentos/state/discord-registry.json
.agentos/state/plan-*.md
.agentos/state/*.json
.agentos/state/*.yaml
.agentos/state/litellm.config.yaml
.agentos/state/*.pids
.agentos/stops/

# Local AgentOS backups (zipped runtime snapshots; may contain .env)
.agentos/backups/
.agentos/logs/

# TypeScript incremental build info
*.tsbuildinfo

# Local IDE / test artifacts
.cursor/
test-results/
playwright-report/

# Website scraper job output
apps/api/data/scraper/
