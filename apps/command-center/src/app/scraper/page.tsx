import { ScraperWorkbench } from "../../components/scraper/ScraperWorkbench";

export const metadata = {
  title: "Rendered Runtime Gallery | AgentOS",
  description: "Capture difficult modern websites, auto-sort their assets and motion systems, and publish them into a showcase gallery."
};

export default function ScraperPage() {
  return <ScraperWorkbench />;
}
